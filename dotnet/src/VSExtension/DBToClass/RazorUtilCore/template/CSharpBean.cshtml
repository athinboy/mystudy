@using System.IO
@using System
@using System.Collections.Generic
@using System.Collections
@inherits RazorEngineCore.RazorEngineTemplateBase<Org.FGQ.CodeGenerate.Model.CSharpBeanModel>
using FreeSql.DataAnnotations;

namespace @Model.FullNamespace
{    
@    [Table(Name = "@Model.CSharpClass.EntityTable.TableName")]
    public class @Model.CSharpClass.ClassName
    {

@foreach (var field in  Model.CSharpClass.Fields)
{
@:        [Column(Name = "@field.Column.Name" @(field.Column.IsPrimaryKeyColumn() ? " ,IsPrimary = true" : ""))]
@:        public @field.FiledTypeStr @field.GetPropertyName() { get; set; }
}

    }

}

 